#include <utility>
#include <vector>

long long int factorial(int number);
std::vector<long long int> pascalTriangle(int row);

double HeronFormula(double a, double b, double c);

std::pair<double, double> delta(double a, double b, double c);

std::vector<std::pair<double, double> > compressSets(std::vector<std::pair<double, double> > sets);

std::pair<double,double> calcTwoUnknowns(double a,double  b,double  c,double  A,double  B,double  C);
